MACHINE_FILE="$HOME/dotfiles/.bashrc.d/machines/$(hostname -s).sh"
[ -f "$MACHINE_FILE" ] && source "$MACHINE_FILE"

for f in "$HOME/dotfiles/.bashrc.d"/[0-9]*.sh; do
    [ -f "$f" ] && source "$f"
done
